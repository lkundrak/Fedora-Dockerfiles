dockerfiles-fedora-network-manager
==================================

Fedora dockerfile for systemd & NetworkManager

To build:

Copy the sources down and do the build:

```
# docker build -t network-manager .
```

To run docker it, do:

```
# docker run --privileged -ti network-manager
```
